# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# compiled binaries (bun build --compile)
/finch
/finch-dev-build

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# planning/orchestration docs — kept on local disk only, not tracked in the repo
docs/

# git worktree checkouts — local only, not tracked in the repo
.worktrees/
